
var every = 100;
var signaliste = 10;
setInterval(() => {/*
      if (game.train.meter >= signaliste) {
            if (game.maps.signalisation.setAs <= 1) {
                  var random = [0](((Math.random() * 2)+1)+"") // get the first number 
                  game.maps.signalisation.setAs = Number.parseInt(random);
                  alert(game.maps.signalisation.setAs)
            }
            if (game.maps.signalisation.setAs == 0) {
                  alert("none :]")
                  game.maps.signalisation.setAs--;
            }
            
            if (game.maps.signalisation.setAs == 1) {
                  alert("euuuh");
                  game.maps.signalisation.setAs++;
            }
            
            if (game.maps.signalisation.setAs == 2) {
                  alert("STOOOOP")
                  game.maps.signalisation.setAs++;
            }
            
            signaliste += every;
      }*/
},15)